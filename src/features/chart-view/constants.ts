/**
 * Constants for chart view configuration
 */

/** Maximum number of additional Y columns for small multiples comparison */
export const MAX_SMALL_MULTIPLES_COLUMNS = 4;

/** Delay in ms before rendering charts in modal to allow ResponsiveContainer to measure correctly */
export const MODAL_CHART_RENDER_DELAY_MS = 300;

/** Minimum height in pixels for each chart in small multiples view */
export const SMALL_MULTIPLES_MIN_CHART_HEIGHT = 120;

/** Maximum character length for X-axis labels before truncation */
export const X_AXIS_LABEL_MAX_LENGTH = 12;
